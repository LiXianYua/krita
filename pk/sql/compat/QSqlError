#pragma once
#include "../PkSqlError.h"

// 兼容垫片：让真实调用点的 `#include <QSqlError>` 在一个字都不改的前提下
// 解析到 PkSqlError。文件名刻意没有扩展名 —— 调用点写的就是 <QSqlError>。
//
// 用宏而不是 `using`：理由与 compat/QSqlDatabase 相同（前置声明兼容）。
#define QSqlError PkSqlError
