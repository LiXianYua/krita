#pragma once

// 编译期占位 —— QPainterPath 不在任何 R 任务的认领范围内。`kis_algebra_2d.h`
// （真品）里的 `VectorPath` 类（`m_originalPath` 成员，`asPainterPath()`
// 返回值/`VectorPath(const QPainterPath&)` 构造参数）与一批自由函数声明
// （`smallArrow`/`getOnePathFromRectangleCutThrough`/`trySimplifyPath`/
// `isInsideShape` 等）用到，候选 B 试接（`testInitInfoXMLClone`）不触达
// `VectorPath`、也不调用任何一个这些自由函数——只需要类型检查通过。
class QPainterPath {
public:
    QPainterPath() = default;
};
