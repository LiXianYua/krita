#pragma once
#include "../PkXmlImplementation.h"
#include "QDomNode"

// 兼容垫片：让真实调用点的 `#include <QDomDocumentType>` 在一个字都不改的
// 前提下解析到 PkXmlDocumentType。文件名刻意没有扩展名 —— 调用点写的就是
// <QDomDocumentType>。PkXmlDocumentType 定义在 PkXmlImplementation.h 里
// （"最小实现"与 PkXmlImplementation 放在一起，见该头文件顶部注释）。
//
// 用宏而不是 `using`：理由与 pk/string/compat/QString 相同——前置声明兼容。
#define QDomDocumentType PkXmlDocumentType
