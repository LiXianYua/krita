#pragma once
#include "../PkXmlNode.h"

// 兼容垫片：让真实调用点的 `#include <QDomNode>` 在一个字都不改的前提下解析到
// PkXmlNode。文件名刻意没有扩展名 —— 调用点写的就是 <QDomNode>。
//
// 用宏而不是 `using QDomNode = PkXmlNode;`：与 pk/string/compat/QString 同因，
// Krita 头里常见 `class QDomNode;` 前置声明，类型别名遇到它会报
// "QDomNode redeclared as different kind of entity"；宏则把那一行原样改写成
// `class PkXmlNode;`，前置声明照样成立。
#define QDomNode PkXmlNode
