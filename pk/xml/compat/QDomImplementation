#pragma once
#include "../PkXmlImplementation.h"

// 兼容垫片：让真实调用点的 `#include <QDomImplementation>` 在一个字都不改的
// 前提下解析到 PkXmlImplementation。文件名刻意没有扩展名 —— 调用点写的就是
// <QDomImplementation>。
//
// 用宏而不是 `using`：理由与 pk/string/compat/QString 相同——前置声明兼容。
#define QDomImplementation PkXmlImplementation
