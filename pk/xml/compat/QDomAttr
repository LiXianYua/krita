#pragma once
#include "../PkXmlAttr.h"
#include "QDomNode"

// 兼容垫片：让真实调用点的 `#include <QDomAttr>` 在一个字都不改的前提下解析到
// PkXmlAttr。文件名刻意没有扩展名 —— 调用点写的就是 <QDomAttr>。
//
// 用宏而不是 `using`：理由与 pk/string/compat/QString 相同——前置声明兼容。
#define QDomAttr PkXmlAttr
