#pragma once
#include "../PkConfigGroup.h"
// 兼容垫片：真实调用点的 `#include <KConfigGroup>`（CamelCase，KF5 转发头风格，
// 无扩展名）在一个字都不改的前提下解析到 PkConfigGroup。理由同 pk/log/compat/QDebug：
// Krita 头文件里有 `class KConfigGroup;` 前置声明（例：libs/command/KisCumulativeUndoData.h:12），
// #define 能把它原样改写成 `class PkConfigGroup;`，using 做不到。
#define KConfigGroup PkConfigGroup
