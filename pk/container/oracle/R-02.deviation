# R-02：Pk 容器族与 Qt 容器族的已知行为偏离
#
# 格式：<api>\t<tag>\t<≥20 UTF-8 码点理由>，tab 分隔，# 开头为注释。
# tag 由**对拍源的作者**按根因命名。
#
# **出现过的每一个 DIFFTAG 都必须在这里有一行**，否则就是「没人判断过它可不可
# 接受」。反过来，这里声明了但这次没观察到的，说明白名单过期、或对拍根本没走到
# 那条路径 —— 两种都该查。
#
# ── tag 长什么样，为什么 ──────────────────────────────────
# tag = `<输入形态>/<shared|unshared>/<elem-int|elem-str>`，三段都由**触发这次
# 差异的输入**构造，绝不是「每个 API 一个字面量常量」。
#
# R-01 第一版就是「每个 API 一个常量」，后果实测过：14 个 API 里 7 个每一次比对
# 都预先落在白名单里、完全退出对拍，注入三组真 bug 全部绿灯通过。**声明一个 tag
# 等于把那条路径上的全部行为永久豁免**，所以豁免面必须切到最小的那一格。
#
# 第二条同样重要：**谓词不能比理由宽。** 下面那条的理由说「替代品原样返回」，
# 谓词里就真的带上了「Pk 侧返回值 == Pk 侧输入」这一项（difftest_seq.cpp 的
# `pkUnchanged`）。少了它，将来某个 bug 让空 before 下返回**别的**东西，也会被
# 这条白名单罩住 —— 那正是 R-01 `toDouble/failure-value` 踩过的坑。
#
# ── 对齐口径 ──────────────────────────────────────────────
# **默认全对齐。** 与 Qt 的任何行为差异默认都是缺陷，应该去改实现，而不是写进
# 这里了事。

# ══════════════════════════════════════════════════════════
# 覆盖度限制 —— **说不出覆盖不到什么的对拍，说明还没想清楚**
# ══════════════════════════════════════════════════════════
#
# 1. 越界 at() / operator[]() / 空容器 first()/last()/takeFirst() **不可对拍**。
#    理由不是「会崩」，是 **Qt 在这里没有定义行为**。真 Qt 5.15.7 实测：
#        默认构建(Q_ASSERT 生效)   at(5) / [](5) / at(-1) / 空容器 first() → rc=134 abort
#        -DQT_NO_DEBUG (release)   同样四项                               → rc=0，返回 0，活着跑完
#    release 下不崩比崩更麻烦：两侧各自返回不同的垃圾值会产生**虚假 mismatch**，
#    而那是未定义的格子。所以 planSeq() 给它们带了 run=false 的守卫，
#    **输入集里根本不生成这些调用**。可以对拍的越界形态由 value(i) / value(i,def) /
#    indexOf(t,from) / lastIndexOf(t,from) 承担（这四个对任意下标都有定义）。
#    **这条只管越界。** 非 const `operator[]` 的**范围内写入**（`c[i] = x`，
#    0 <= i < size）在指令表里（api 名 `bracket-write`）—— 它是 COW 最容易漏的
#    写入口，只有共享用例的 alias dump 能让「写穿」现形（自证表 D 那组就是它）。
#    关联侧同理：`m[k]`（bracket-read，缺 key 时插默认值）与 `m[k] = v`
#    （bracket-write）两条都在表里。
#
# 2. QHash / QSet 的**迭代顺序不可对拍**。Qt5 的 QHash 带随机化种子，
#    std::unordered_map 又是另一套；同一段代码 Qt 5.15.7 打出 6,7,4,5,2,3,0,1，
#    5.15.13 打出 4,5,6,7,0,1,2,3 —— 跨补丁版本就变。hash/set 通道只比**排序后的
#    集合**，「顺序」这一维完全没有覆盖。连带两处也退化了：
#      · hash 的 `erase(find(k))` **返回值**一格都没比**（「下一个是谁」、乃至
#        「是不是恰好到头」都跟着顺序走。第一版拿 end/not-end 去比，跑出 6646 次
#        全是假差异；现在无序通道下只比 erase 之后的状态）
#      · hash 的 `key(v)` 只在「至多一个 key 映射到 v」时才比（valueOwners<=1）
#    map 通道相反：QMap 与 std::map 都按 operator< 排序，迭代顺序**可观察且该一致**，
#    所以按迭代顺序直接比 —— 这条正好压住 PkString::operator< 与 QString::operator<
#    是不是同一个口径（token 表里 U+FFFD 与 🎨 那一对，码元序与码点序结论相反）。
#
# 3. **迭代器失效时机不可对拍**：失效后使用是 UB，两侧各自的垃圾不构成差异。
#
# 4. **自定义元素类型不可对拍**：只能用两侧都有的类型。本轮元素类型是
#    int 与 QString/PkString 两种，key 类型同。
#
# 5. **QList<T> 在 Qt5 对大对象退化成指针数组**是内存布局，不可观察，
#    对拍证明不了这一层的等价。
#
# 6. **负数 reserve(n) / resize(n) 没有生成**：替代品会把负数转成 size_t
#    去要 2^64 字节（不是差异，是资源炸弹）。守卫写在 planSeq 里。
#
# 7. **API 面本身不全的那几格没有对拍对象**（不是行为差异，是缺方法，
#    模板实例化时就编不过，所以指令表里没有它们）：
#      QVector/QList::mid()、QVector::startsWith()/endsWith()、
#      QList::swapItemsAt()、QSet::intersects()、QMap::unite()/insertMulti()。
#      要不要补由 R-13 按调用点定。
#
# 8. `QStringList` 只跑了 join / filter / sort / removeDuplicates /
#    replaceInStrings 五个专属方法，正则相关的重载（QRegExp / QRegularExpression）
#    整片不在替代品范围内。
#
# 9. **指令表覆盖面：保留范围里哪些 API 有对拍把守、哪些只有单测把守。**
#    前 8 条讲的都是「什么形态不可对拍」，这一条讲的是「哪些 API 压根没进指令
#    表」——两件事，前者是结构性的做不到，后者是这一轮**选择**没做。
#    区别对 R-13 / S-02 是实打实的：**「单测把守」证明的是「行为符合我以为的
#    样子」，「对拍把守」证明的是「行为和真 Qt 5.15.7 一样」。** 机械替换的调用
#    点落在只有单测把守的那一格上时，风险等级不同。
#
#    口径：「进表」= 这一轮 ORACLE-COVER 里有它、比对次数 > 0。清单按替代品的
#    **保留 API 面**枚举（不在保留面里的见第 7 条，那是缺方法，不是没测）。
#
#    ── 进了指令表（对拍把守）────────────────────────────────
#    序列侧  append / prepend / operator<< / insert(int,T) / clear / size /
#            isEmpty / at / operator[] 写(bracket-write) / value / value(i,def) /
#            indexOf / lastIndexOf / contains / count(T) / first / last /
#            erase(单点) / reserve / append(other) / append(self) / 自赋值 /
#            operator== ；PkVector remove(i) / remove(i,n) / resize / fill /
#            fill(t,n) ；PkList removeAt / removeAll / removeOne / removeFirst /
#            removeLast / takeAt / takeFirst / takeLast / move ；
#            PkStack push / pop / top ；PkQueue enqueue / dequeue / head ；
#            PkStringList join(PkString) / filter / sort / removeDuplicates /
#            replaceInStrings
#    关联侧  insert / remove / take / value / value(k,def) / contains / count(K) /
#            keys / keys(v) / values / values(k) / key(v) / key(v,def) /
#            operator[] 读(缺 key 时插默认值) / operator[] 写 / clear / size /
#            isEmpty / find / constFind / erase(find(k)) / 自赋值 / operator==
#    集合侧  insert / remove / contains / size / count() / isEmpty / clear /
#            values / unite / intersect / subtract / operator|= / operator== /
#            自赋值 / unite(self) / intersect(self) / subtract(self)
#
#    ── 没进指令表（**只有单测把守**）──────────────────────
#    序列侧  const operator[](int) const · count()（无参）· swap(Derived&) ·
#            data() / constData() · STL 别名族 front / back / push_back /
#            push_front / pop_back / pop_front / empty · insert(迭代器版) ·
#            erase(区间版) · operator!= · operator+=(T) / operator+=(Derived) ·
#            迭代器本身 begin / end / cbegin / cend / constBegin / constEnd /
#            rbegin / rend · PkVector::capacity / PkVector::toList /
#            PkList::toVector · PkStringList::join(char16_t) 与
#            filter / sort / replaceInStrings 的 PkCaseSensitivity 重载
#    关联侧  const operator[](K) const（**按值返回、不插入**，与进表的非 const 版
#            语义不同）· count()（无参）· empty() · operator!= ·
#            erase(pos) 的直接调用（进表的是 erase(find(k)) 这个组合）·
#            PkMap::lowerBound / upperBound（四个重载全部）·
#            迭代器 begin / end / cbegin / cend / constBegin / constEnd 本身
#            （迭代器的 key() / value() 只在 find / constFind / erase-find 的
#            返回值里间接受检，没有独立形态）
#    集合侧  toList()（values() 的别名，进表的是 values）· operator!= ·
#            begin / end / constBegin / constEnd
#    两侧共有 PkIsSharedWith / PkUseCount —— **不可对拍**，不是漏了：这两个是
#            替代品自有的 COW 探针，Qt 没有对应物（QVector::isDetached 语义不同
#            且实测 0 调用点）。COW 这一维靠 shared 用例的 alias dump 把守。
#
# ══════════════════════════════════════════════════════════
# 判据有效性自证：五组注入实验（改完即刻还原，pk/ 下无残留）
# ══════════════════════════════════════════════════════════
# 基线 total=3447803 mismatch=0，出现 tag 0 个、未声明 0 个。
# 五组数字**全部在同一份指令表下重测过**（`bracket-write` 进表之后 total 从
# 267 万涨到 344 万，旧那一版的数字与新基线不可比，不能只换一行）。
#   A value() 越界返末元素 / value(i,def) 忽略 def   mismatch  47810  未声明 tag 90
#   B pkRemoveAt off-by-one                          mismatch 104005  未声明 tag 40
#   C indexOf 命中时恒返 0                            mismatch  22218  未声明 tag 84
#   D 非 const operator[] 不 detach（**只破 COW**）   mismatch   9215  未声明 tag  9
#   E PkSet::intersect 条件取反                       mismatch  37940  未声明 tag 12
# （mismatch 含基线那 36 次已声明的 replaceInStrings；「未声明 tag」数的是
#  DIFFTAG 行数，不是命中次数。D 那 9 条合计命中 9179 次。）
#
# D 是刻意挑的：它不改返回值、也不改容器自身的状态，只有 shared 用例里的
# alias dump 会现形 —— 用来证明「共享」这一维不是摆设。**它的 tag 数最少（9），
# 那不是判据弱，是形态空间本来就窄**：写穿只可能发生在 `shared` × `idx-in-range`
# 这一格，剩下的只是「哪个容器 × 元素是 int 还是 str」，5 × 2 减去 PkStringList
# 没有 elem-int 通道，正好 9 条：
#   vector/list/stack/queue/stringlist . bracket-write  idx-in-range/shared/elem-{int,str}
# **注入形态与指令表必须对得上**：D 注入的是 PkArrayContainer.h 的
# `T &operator[](int)`（把 `m_d.PkMut()` 换成 `const_cast<PkInner &>(m_d.PkConst())`），
# 而序列侧指令表在本轮之前**没有非 const operator[] 写入这条指令**，同样的注入
# 当时一个未声明 tag 都产不出来。补上 `bracket-write` 才让这组自证真正成立。
