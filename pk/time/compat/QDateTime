#pragma once
#include "../PkDateTime.h"

// 兼容垫片：让真实调用点的 `#include <QDateTime>` 在一个字不改的前提下解析到
// PkDateTime。文件名刻意没有扩展名 —— 调用点写的就是 <QDateTime>。
// `Qt::ISODate`/`Qt::RFC2822Date`/`Qt::ISODateWithMs` 三个 constexpr 别名值已由
// PkDateTime.h 提供（见该文件底部 namespace Qt 块），这里不再重复定义。
#define QDateTime PkDateTime
