# pk/geometry 与真 Qt5 的已声明差异清单
#
# 格式：<api>\t<tag>\t<期望计数>\t<≥20 UTF-8 码点理由>，**tab 分隔**，# 开头为注释。
# tag 由对拍源的作者按根因命名，run_oracle.sh 不解释它，只做核对：
#   · 出现过的 DIFFTAG 必须在这里 —— 否则 FAIL（**未声明的差异 = 没人判断过
#     它可不可接受**）
#   · **观察到的计数必须等于这里写的期望计数** —— 否则 FAIL（见下面「为什么有
#     第三列」）。**没观察到 = 实得 0**，同样按这条判：声明 500 次实得 0 次与
#     实得 499 次一样 FAIL（第三列是**额度**，用少了也说明行为变了没人判断过）
#   · 只有**额度写着 0** 又确实没观察到的才出 WARN —— 那种行对闸门不起作用
#     （该 tag 真出现时，声明了走计数漂移、没声明走未声明，两条都 FAIL），
#     提醒删掉而已
#
# ══════════════════════════════════════════════════════════════════════════
# **为什么有「期望计数」这一列（Task 4 修复轮，评审 Important 1）**
#
# 在此之前 run_oracle.sh 只核对 (api, tag) 这个**键**在不在清单里，计数只打印、
# 不判定 —— 也就是说**一条已声明的 tag 是张无限额度的白名单**：同一个 tag 下
# 差异从一千条涨到一百万条，闸门一声不吭。
#
# 这不是假设。评审员实测（I8）：把 4 个 .so 侧 API 的翻正判据全部加宽成
# `(long long)x2 - x1 + 1 < 0`（16 处源码），与真 Qt 的分家从 704 589 抬到
# **1 485 313（2.1 倍）**，`run_tests.sh` 与 `run_oracle.sh` **双双 exit=0 放行**。
# 当时唯一发现它的办法是人盯着那个数字 —— 那不是判据，那是运气。
#
# 这是 Task 3 那个洞换一层重演（那次是「漏一个 rec() 没人报」，这次是
# 「数字翻倍没人报」），所以同样要用机器堵：**计数写进清单，漂移即 FAIL**。
#
# 三条 canary 的期望计数都是 1：它们各是一次故意不相等的比对，跑一次、红一次。
#
# ══════════════════════════════════════════════════════════════════════════
# **R-03 至今（截至 Task 4 修复轮）零真实偏离 —— 这是正确结果，不是漏测。**
#
# 与 R-01 的根本不同：PkString 是重写，行为差异不可避免；而 PkPoint/PkPointF、
# PkSize/PkSizeF、PkRect 都是**逐字照抄 Qt 头文件与实现**（连 qAbs(INT_MIN)
# 回绕、int(inf)、QSize() == (-1,-1)、isEmpty 的 `<1` 与浮点版 `<=0.` 不一致、
# scaled 在退化源上直接返回目标、qint64 中间量窄化回绕、QRect 的差一语义与
# normalized 的 `x2 < x1-1` 交换边界都照抄），逐输入对拍下来本来就该是零差异。
#
# 逐 Task 的比对规模（**历史陈述，各是当时那一轮的实测值，不跟着后续基线漂**）：
#   · 截至 Task 2（Point 族）      total =  35 569 662，mismatch = 3（canary）
#   · 截至 Task 3（+ Size 族）     total =  98 759 499，mismatch = 3（canary）
#   · 截至 Task 4 修复轮（+ Rect） total = 125 338 365，mismatch = 3（canary）
# 判别力靠注入自证，不靠这些数字本身 —— 数字只说明"喂进去的输入不是几条"。
#
# canary 是三条故意不相等的比对，走的是与真实 API 完全相同的 rec()/比较原语/
# tag 构造路径 —— 它们的作用不是记录偏离，而是证明「mismatch 这个数字是活的」：
# 一旦比较管道被写死、被优化掉、或者 tag 构造断了，canary 会**消失**，
# run_oracle.sh 就 FAIL。
#
# ══════════════════════════════════════════════════════════════════════════
# **Task 4 一度写下过 9 条非 canary 行（`span-overflow-ub`），修复轮把它们删了。**
#
# 那 9 行声称的是：`operator|` / `operator&` / `contains(QRect)` / `intersects`
# （及转发到它们的 united / intersected / |= / &=）在"跨距越出 int"的输入上与
# Qt 分家 704 589 次，根因是**旗标不对等** —— 这四个的实现编在 libQt5Core.so
# 里，对拍 TU 的 `-fwrapv` 到不了那侧，同一条 `x2 - x1 + 1 < 0` 判据两侧取值不同。
#
# 根因诊断是对的，处置是错的：既然根因是旗标，就该**把旗标改掉**，而不是把
# 那片输入白名单化。修复轮去掉了对拍侧的 `-fwrapv`（库与单测那份保留，`-Os`
# 那一档需要它），同一份源码、同一批输入 mismatch 直接回到 **3（只剩 canary）**。
#
# 代价这样记：那 9 行原本豁免的是 **3 286 575 次比对（占 total 的 2.62%，实测
# 计数）** —— 注意不是 704 589，那是**差异**次数；当初的报告拿差异次数当盲区
# 规模写，把它低估了 4.7 倍。这正是「豁免档必须量化、而且要量对量」的理由。
# 现在这 328 万次是真比对，任何差异都会是新 tag 而 FAIL。
#
# 换来的新代价（登记在 README 覆盖度缺口）：溢出输入上"两侧一致"从"被旗标钉死"
# 变成了"同编译器同旗标下的巧合"，换一个 GCC 编出来的 Qt 可能冒出新 tag ——
# **但那是 FAIL（响的）不是静默放行，失败方向是对的。**
#
# ══════════════════════════════════════════════════════════════════════════
# ⚠ 抄这份骨架的后续 Task（RectF / Transform）注意：
#   · 往这里加行之前先问「这条差异是不是缺陷」。默认答案是**是**，回去改替代品。
#     唯一站得住的偏离理由是决策文档已明确划在范围外的东西。
#   · 加行之后回头把理由里的**每个限定词**对一遍谓词（方法论规则二）。理由说
#     「退化矩形」，谓词就不能是「任何矩形」——宽一格就等于把那一整片行为
#     永久白名单化，注入真 bug 也看不见。
#   · **谓词宽度对了还不够，额度也要对**：第三列的期望计数就是额度。写下一条
#     真实偏离时，那个数字要能说清"为什么恰好是这么多"，不能填个大概。
#   · 差异如果来自**两侧编译旗标不一样**，那不是偏离，是装置缺陷 —— 先把旗标
#     对齐（Task 4 修复轮就是这么收的），对不齐再谈声明。
# ══════════════════════════════════════════════════════════════════════════

canary	int-value	1	比较管道自证：故意拿 QPoint(1,2) 与 PkPoint(1,3) 相比，整数分量比较与计数链路活着才会出现这一条，消失即判据失效
canary	double-bits-signed-zero	1	比较管道自证：+0.0 与 -0.0 按位不同但 == 判等，这一条能出现才说明 same_double 走的是 memcpy 位比较而不是退化成了 ==
canary	nan-vs-number	1	比较管道自证：NaN 与 1.0 必须判成不同，这一条能出现才说明 NaN 分支没有把一切都判成相同（NaN 判等只在两侧都是 NaN 时成立）
# ══════════════════════════════════════════════════════════════════════════
# **Task 6（Transform 族）写下了 R-03 的第一条真实偏离** —— 23 行，根因只有一个。
#
# `QTransform::mapRect` 的两个重载在
#   `type() == TxProject` **且** `needsPerspectiveClipping(rect, *this)` 为真
# 时，不走四角包围盒，而是
#     QPainterPath path; path.addRect(rect); return map(path).boundingRect();
# —— 把矩形当路径、在近裁剪面（w = 1e-6）上真的**裁**一刀再取包围盒。
# 落回四角包围盒的话，被夹持到 1e-6 的那个角会把包围盒撑到 1e7 量级，而 Qt
# 裁出来的是 1e6 量级：实测 t(1,0,-1, 0,1,0, 0,0,1) 对 (0,0,10,10)，
# Qt 给 (0,0,999999.0000000007,1e7)，四角包围盒给 (0,0,1e7,1e7)。
#
# **这一支在代码里是显形的**：PkTransform.cpp 的两个 mapRect 保住了 Qt 原本的
# 四分支结构（`t <= TxTranslate` / `t <= TxScale` /
# `t < TxProject || !pkNeedsPerspectiveClipping(...)` / else），最后那一支
# 就是本条偏离，注释直接指到这里。合并分支会省掉几行、也会让这个洞消失在视野里。
#
# **为什么这条站得住（唯一站得住的偏离理由：决策文档已明确划在范围外）**：
# `QPainterPath` 不在 `Qt替代品选型.md` §1 几何那一行点名的四个类型
# （QRect/QPointF/QSize/QTransform）里，**归属未定**，R-03.md §覆盖度缺口
# 把它与 QLineF/QPolygonF/QRegion 一起列为"要报回主会话的归属问题"
# （实测 766 次 / 168 文件，是一个独立子系统，不是一个函数）。
# 它不是"我们写错了"，是"这一跳的被调方还没有人认领"。
#
# **谓词与理由逐个限定词对齐（规则二）**：tag 只在
#   tfFreshIsProject(m)  ——`!qFuzzyIsNull(m13) || !qFuzzyIsNull(m23) || !qFuzzyIsNull(m33-1)`，
#                          与 Qt 的 type() 第一档**逐字相同**的模糊门槛
#   && tfNeedsClip(m, l, r, t, b) —— qtransform.cpp:1934-1940 的就地重算，
#                          连 qMin 用 `(a<b)?a:b` 而不是 std::fmin 都照抄
#                          （NaN 上两者取值不同，而这里真会吃到 NaN）
# 同时成立时才构造，**两个限定词都在，一个不多**。两侧都是拿**输入的九个 double**
# 重算的，不问被测对象的 type() —— 问它的话被测对象坏掉时 tag 会跟着坏。
#
# ══════════════════════════════════════════════════════════════════════════
# **额度「为什么恰好是这么多」怎么读**
#
# 这 23 行的额度是可推导的，靠两样东西：
#
# ① **对拍程序多打一种行：`DIFFDEN <api> <tag> <分母>`** —— 该 tag 的谓词
#    命中过多少次比对（分子只数分家的那些）。`run_oracle.sh` 把它印在结论块里，
#    于是每一行都能读成「**命中 N 次里分家 M 次**」。分母不进闸门（第三列仍然
#    只比分子），它是给人推导用的：分子孤零零一个数，既可能是"喂了 12 800 次
#    几乎全分家"，也可能是"喂了 200 万次只有这么点"，两者对偏离范围的含义
#    天差地别。
# ② **tag 同时带矩阵形态与矩形形态**（照「一个 API 的 shape 由它全部参与分量
#    算出」这条约定；只带矩阵形态的话会裂不开）。于是有 23 行，
#    每一行的分母都小到能对着输入集数出来。
#
# **实测：这 23 行的分母合计 27 492、分子合计 25 495。**
# （run_oracle.sh 结论块末尾打的是 27 495 / 25 498 —— 那是**含三条 canary**的
#  合计，差的正好是那 3 条。两个数字都对，口径不同，别拿来互相对账。）
# **23 行里有 12 行分子 == 分母**（命中即分家，合计 1 547 次）—— 那几片输入上
# 被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，没有一次与 Qt 的裁剪结果相同。
# 剩下 11 行分子 < 分母，差额（27 492 - 25 495 = 1 997 次）是**裁剪前后包围盒
# 重合**的输入：被夹持的那个角落在已有包围盒之内，裁不裁都不改变四个边界。
#
# 分母 N 自己的来源是数得出来的：输入是固定的（Group 5 的缩减扫描集 3 778 个
# 矩阵 × kTfRectF 的 12 个浮点矩形 / kTfRectI 的 10 个整数矩形），
# 命中「投影且需裁剪」的组合数由 tfFreshIsProject × tfNeedsClip 两个谓词唯一确定。
#
# ⚠ **这 23 行不豁免任何别的东西。** Transform 一节跑了 29 020 413 次比对
# （= 全量 154 358 778 减去 Transform 之前四族的 125 338 365），除了这 25 495 次之外
# **一次都没分家** —— 包括惰性缓存那一整类**多步序列**（四个标量运算符各自的
# 过期路径 × 问过/没问过两条分支、旋转往返、连续 mutator 链）、直角特判、
# inverted 的三条路径、map 四个重载的夹持与不夹持。
# ══════════════════════════════════════════════════════════════════════════

T::mapRect(PkRect)	persp-clip/proj-finite/negative-unit	65	整数版 mapRect，矩阵九分量全是有限非零值、一轴负跨距一轴单像素。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 65 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRect)	persp-clip/proj-finite/normal-normal	428	整数版 mapRect，矩阵九分量全是有限非零值、两轴都是正常区间。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 428 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRect)	persp-clip/proj-finite/span-overflow-span-overflow	44	整数版 mapRect，矩阵九分量全是有限非零值、两轴跨距都越出 int。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 194 次里分家 44 次；另外 150 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRect)	persp-clip/proj-finite/unit-unit	40	整数版 mapRect，矩阵九分量全是有限非零值、两轴都是单像素。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 40 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRect)	persp-clip/proj-finite/zero-zero	32	整数版 mapRect，矩阵九分量全是有限非零值、两轴跨距都为零。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 32 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRect)	persp-clip/proj-zero/negative-unit	1225	整数版 mapRect，矩阵含 +0.0 分量、一轴负跨距一轴单像素。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 1284 次里分家 1225 次；另外 59 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRect)	persp-clip/proj-zero/normal-normal	7104	整数版 mapRect，矩阵含 +0.0 分量、两轴都是正常区间。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 7313 次里分家 7104 次；另外 209 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRect)	persp-clip/proj-zero/span-overflow-span-overflow	1603	整数版 mapRect，矩阵含 +0.0 分量、两轴跨距都越出 int。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 2549 次里分家 1603 次；另外 946 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRect)	persp-clip/proj-zero/unit-unit	1110	整数版 mapRect，矩阵含 +0.0 分量、两轴都是单像素。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 1183 次里分家 1110 次；另外 73 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRect)	persp-clip/proj-zero/zero-zero	1757	整数版 mapRect，矩阵含 +0.0 分量、两轴跨距都为零。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 1883 次里分家 1757 次；另外 126 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRectF)	persp-clip/proj-finite/finite	246	浮点版 mapRect，矩阵九分量全是有限非零值、四个字段全是有限非零值。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 246 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRectF)	persp-clip/proj-finite/nonfinite	80	浮点版 mapRect，矩阵九分量全是有限非零值、含 inf 或 nan 字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 80 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRectF)	persp-clip/proj-finite/signed-zero	32	浮点版 mapRect，矩阵九分量全是有限非零值、含 -0.0 字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 32 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRectF)	persp-clip/proj-finite/subnormal	32	浮点版 mapRect，矩阵九分量全是有限非零值、含次正规字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 32 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRectF)	persp-clip/proj-finite/zero	306	浮点版 mapRect，矩阵九分量全是有限非零值、含 +0.0 字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 306 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRectF)	persp-clip/proj-signed-zero/finite	2	浮点版 mapRect，矩阵含 -0.0 分量、四个字段全是有限非零值。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 3 次里分家 2 次；另外 1 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRectF)	persp-clip/proj-signed-zero/subnormal	1	浮点版 mapRect，矩阵含 -0.0 分量、含次正规字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 1 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRectF)	persp-clip/proj-signed-zero/zero	4	浮点版 mapRect，矩阵含 -0.0 分量、含 +0.0 字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 4 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRectF)	persp-clip/proj-zero/finite	4828	浮点版 mapRect，矩阵含 +0.0 分量、四个字段全是有限非零值。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 4945 次里分家 4828 次；另外 117 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRectF)	persp-clip/proj-zero/nonfinite	281	浮点版 mapRect，矩阵含 +0.0 分量、含 inf 或 nan 字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 281 次**全部**分家（分子=分母）：这一片输入上被夹到 1e-6 的角必定把包围盒撑到 1e6 量级以上，四角包围盒与 Qt 裁出来的结果没有一次相同
T::mapRect(PkRectF)	persp-clip/proj-zero/signed-zero	598	浮点版 mapRect，矩阵含 +0.0 分量、含 -0.0 字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 720 次里分家 598 次；另外 122 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRectF)	persp-clip/proj-zero/subnormal	719	浮点版 mapRect，矩阵含 +0.0 分量、含次正规字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 720 次里分家 719 次；另外 1 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
T::mapRect(PkRectF)	persp-clip/proj-zero/zero	4958	浮点版 mapRect，矩阵含 +0.0 分量、含 +0.0 字段。根因：Qt 在 type()==TxProject 且需透视裁剪时走 QPainterPath，QPainterPath 归属未定不在 R-03 范围，本类落回四角包围盒。命中 5151 次里分家 4958 次；另外 193 次两侧恰好相同 —— 那是裁剪前后包围盒重合的输入（被夹持的角落在已有包围盒之内，裁不裁都不改变四个边界）
# ══════════════════════════════════════════════════════════════════════════
# **R-21 T2（PkPolygonF）写下的第二条真实偏离** —— 1 行，根因与 mapRect 的
# 23 行是同一个：`QPainterPath` 不在 R-21 交付范围（`Qt替代品选型.md` §1 几何
# 那一行点名的十个类型里没有它，归 R-22）。
#
# `QTransform::map(const QPolygonF&)` 在 `type() >= TxProject` 时，真 Qt 把
# 多边形铺进 `QPainterPath` 做透视裁剪（近裁剪面 w=1e-6 之外的顶点被裁掉/
# 替换成新的交点），本类落回**逐点无裁剪**的 `map(const PkPointF&)`——它自己
# 的 TxProject 分支就是 `1/(m13*x+m23*y+m33)` 透视除法，点在近裁剪面之外时
# 得到 inf/翻转坐标而不是被裁掉的新顶点。这一支在 PkTransform.cpp 里显形
# （map(const PkPolygonF&) 上方那段注释直接指到这里），README「覆盖度缺口」
# 同步点名。谓词 `txproject-deviation` = `q.type() >= TxProject`（真 Qt 那侧
# 分档，与 mapRect 用 `tfFreshIsProject` 就地重算是同一条精神：分类借真 Qt 的
# type() 而不是猜本类会给什么档），非退化多边形（n>0）时构造。
# ══════════════════════════════════════════════════════════════════════════
T::map(PolygonF)	txproject-deviation	8	浮点版 map(QPolygonF)，真 Qt 在 type()>=TxProject 时把多边形铺进 QPainterPath 做透视裁剪，QPainterPath 归属未定不在 R-21 范围（归 R-22），本类落回逐点无裁剪的透视除法。命中 14 次里分家 8 次；另外 6 次两侧恰好相同 —— 那是裁剪前后顶点重合的输入（所有顶点都落在近裁剪面之内，裁不裁都不改变坐标）
