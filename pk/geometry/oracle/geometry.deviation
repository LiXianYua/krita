# pk/geometry 与真 Qt5 的已声明差异清单
#
# 格式：<api>\t<tag>\t<≥20 UTF-8 码点理由>，**tab 分隔**，# 开头为注释。
# tag 由对拍源的作者按根因命名，run_oracle.sh 不解释它，只做双向核对：
#   · 出现过的 DIFFTAG 必须在这里 —— 否则 FAIL（**未声明的差异 = 没人判断过
#     它可不可接受**）
#   · 这里声明了但这次没观察到的出 WARN（白名单过期，或对拍没走到那条路径）
#
# ══════════════════════════════════════════════════════════════════════════
# **Point / Size 两族一条真实偏离都没有，这是正确结果、不是漏测。**
# 与 R-01 的根本不同：PkString 是重写，行为差异不可避免；而 PkPoint/PkPointF
# 是**逐字照抄 qpoint.h**（连 qAbs(INT_MIN) 回绕、int(inf) 这类 UB 都照抄），
# 逐输入对拍下来本来就该是零差异。
#
# **Task 3（Size 族）之后仍然是零真实偏离**：PkSize/PkSizeF 同样是逐字照抄
# qsize.h 与 qsize.cpp（连 QSize() == (-1,-1)、isEmpty 的 `<1` 与浮点版 `<=0.`
# 不一致、scaled 在退化源上直接返回目标、qint64 中间量窄化回绕都照抄），
# 98 759 499 次逐输入比对下来 mismatch 仍然只有那 3 条 canary。
# 判别力靠注入自证：5 组变异里 4 组产生了未声明的新 tag（第 5 组是"整数版
# isEmpty 的 <1 改成 <=0"，那两条公式在整数上**本来就等价**，零差异是正确结果）。
#
# canary 是三条故意不相等的比对，走的是与真实 API 完全相同的 rec()/比较原语/
# tag 构造路径 —— 它们的作用不是记录偏离，而是证明「mismatch 这个数字是活的」：
# 一旦比较管道被写死、被优化掉、或者 tag 构造断了，canary 会**消失**，
# run_oracle.sh 就 FAIL。
#
# ══════════════════════════════════════════════════════════════════════════
# **Task 4（Rect 族）写下了 R-03 的第一批非 canary 行 —— 9 条，全部同一个根因。**
#
# 它**不是 PkRect 的行为偏离**，是**对拍装置自身够不到的一块**，证据是决定性的：
#
#   同一份源码、同一批输入（total=125 338 363）：
#     · `-O2 -fwrapv`（现行旗标）  → mismatch = 704 589
#     · `-O2`（去掉 -fwrapv）      → mismatch = **3**（只剩 canary）
#   唯一的变量是编译旗标。也就是说 PkRect 的源码在 Qt 自己的旗标下与 Qt
#   **逐输入完全一致**，一条差异都没有。
#
# 机理：`operator|` / `operator&` / `contains(QRect)` / `intersects` 这四个
# （以及转发到它们的 united / intersected / |= / &=）**不在头文件里，实现编在
# libQt5Core.so 里**（`nm -DC libQt5Core.so.5 | grep 'QRect::operator|'` 看得到）。
# 对拍 TU 的 `-fwrapv` 只作用于头文件里的 inline 代码，**到不了那份 .so**。
# 而这四个的翻正判据写作 `x2 - x1 + 1 < 0`，跨距越出 int 时是有符号溢出 UB：
# 我们这侧按二补数回绕、Qt 那侧按"溢出不会发生"推导，两个取值都合法。
#
# 三条旁证把根因钉死在"旗标"而不是"我们抄错了"：
#   · 同样住在 .so 里、但判据写作 `x2 < x1 - 1` 的 normalized() 与
#     contains(QPoint) **一条 mismatch 都没有**；
#   · 全部 inline 成员（width / isNull / center / adjust / moveTo …）全绿；
#   · 专门写的扫描程序在 9⁸ = 43 046 721 组极值坐标双目输入上确认：
#     **跨距不溢出时分家次数为 0**。
#
# 试过"把判据加宽到 long long 去凑 Qt 那侧"——mismatch 从 704 589 涨到
# 1 485 313，说明 Qt 那侧也不是统一的"不回绕"，是逐点依赖 GCC 在各 inline 点
# 上做了什么。**源码层面无解**，所以登记成偏离而不是回去改实现。
#
# 谓词宽度（方法论规则二）：tag `span-overflow-ub` 的判定就是
# `spanOverflows(a) || spanOverflows(b)`，而 spanOverflows 查的正是
# `x2-x1` 与 `x2-x1+1` 这**两个**中间量是否越出 int —— 与上面这句理由逐字对齐，
# 一格都没多。有定义的那一侧仍然带着完整的细粒度 tag（`defined/<形态对>/<相对
# 位置>`），真出现偏离时必然是**新 tag**，照样 FAIL。
# ⚠ 代价说清楚：这一档等于把"跨距溢出的矩形"上这九个 API 的差异整片豁免了。
# 可接受的理由是**那片输入两侧都是 UB，"正确"在那里没有定义**；不可接受的用法
# 是拿它去盖别的东西 —— 所以谓词不许再放宽一格。
#
# ⚠ 抄这份骨架的后续 Task（Size/Rect/RectF/Transform）注意：
#   · 往这里加行之前先问「这条差异是不是缺陷」。默认答案是**是**，回去改替代品。
#     唯一站得住的偏离理由是决策文档已明确划在范围外的东西。
#   · 加行之后回头把理由里的**每个限定词**对一遍谓词（方法论规则二）。理由说
#     「退化矩形」，谓词就不能是「任何矩形」——宽一格就等于把那一整片行为
#     永久白名单化，注入真 bug 也看不见。
# ══════════════════════════════════════════════════════════════════════════

canary	int-value	比较管道自证：故意拿 QPoint(1,2) 与 PkPoint(1,3) 相比，整数分量比较与计数链路活着才会出现这一条，消失即判据失效
canary	double-bits-signed-zero	比较管道自证：+0.0 与 -0.0 按位不同但 == 判等，这一条能出现才说明 same_double 走的是 memcpy 位比较而不是退化成了 ==
canary	nan-vs-number	比较管道自证：NaN 与 1.0 必须判成不同，这一条能出现才说明 NaN 分支没有把一切都判成相同（NaN 判等只在两侧都是 NaN 时成立）

R::operator|	span-overflow-ub	operator| 编在 libQt5Core.so 里，对拍 TU 的 -fwrapv 到不了那侧；它的翻正判据 x2-x1+1<0 在跨距越出 int 时是有符号溢出 UB，我们回绕、Qt 按无溢出推导。去掉 -fwrapv 重编同一份源码，mismatch 从 704589 降到 3，证明源码本身与 Qt 逐输入一致
R::operator|=	span-overflow-ub	operator|= 是 *this = *this | r 的转发，差异整条来自 operator| 那一跳，根因与旗标口径同 R::operator| 那一行，不另立说法
R::united	span-overflow-ub	united 是 operator| 的具名别名（qrect.h:449），差异逐条与 operator| 同源，跨距不溢出时实测零差异
R::operator&	span-overflow-ub	operator& 同样编在 libQt5Core.so 里，翻正判据 x2-x1+1<0 在跨距溢出时两侧旗标不同取值不同；对照组去掉 -fwrapv 后该 API 零差异
R::operator&=	span-overflow-ub	operator&= 是 *this = *this & r 的转发，差异整条来自 operator& 那一跳，根因与旗标口径同 R::operator& 那一行
R::intersected	span-overflow-ub	intersected 是 operator& 的具名别名（qrect.h:444），差异逐条与 operator& 同源，跨距不溢出时实测零差异
R::intersects	span-overflow-ub	intersects 与 operator& 是 qrect.cpp 里两段同样的判断，同住 .so、同一条 x2-x1+1<0 判据，跨距溢出时旗标差异导致选支不同
R::containsRect	span-overflow-ub/proper-false	contains(QRect,false) 编在 libQt5Core.so 里，翻正判据 x2-x1+1<0 在跨距溢出时两侧旗标不同；proper 两个取值走两条分支，各自一行
R::containsRectProper	span-overflow-ub/proper-true	contains(QRect,true) 与 proper=false 走的是不同的比较分支（<= 而不是 <），单独一行；根因同为 .so 侧够不到 -fwrapv 的有符号溢出 UB
