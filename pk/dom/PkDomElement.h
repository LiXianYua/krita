#ifndef PK_DOMELEMENT_H
#define PK_DOMELEMENT_H
// R-50：PkDomElement 定义见 PkDom.h（值语义句柄 + document 拥有底层节点）。
// 本头仅作锁内文件锚点，便于 `#include <PkDomElement>` 解析到同一类型。
#include "PkDom.h"
#endif // PK_DOMELEMENT_H
