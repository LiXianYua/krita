#ifndef KISALGEBRA2D_QT_SUBSET_H
#define KISALGEBRA2D_QT_SUBSET_H

// S-09-g 扩锁去重：本头原为 libs/global/kis_algebra_2d.h 的 Pk 化子集拷贝，
// 两头同被 canvas TU include 时产生大量重定义。global 版已完整覆盖
// （含 fuzzyMatrixCompare/DecomposedMatrix/norm/leftUnitNormal 等），
// 本头改为纯转发。

#include <kis_algebra_2d.h>

#endif // KISALGEBRA2D_QT_SUBSET_H
