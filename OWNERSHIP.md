# OWNERSHIP · target 占用与算法归属

> **只有人能改这份文件。** agent 不写这里——它把发现记进 `.exec/progress/<ID>.md`（用
> `status.sh note`），验收时由人转录过来。理由见文末「谁来写」。
>
> 依据：`$PK/docs/迁移执行计划.md` §6.7「代码所有权」三条硬规则。

`PK = /mnt/ssd-disk/liyang/projects/paint_tips`

## 这份文件回答两个问题

| | 问题 | 对应硬规则 | 现在还需要吗 |
|---|---|---|---|
| **§1** | 这个 target 现在谁在动 | 硬规则一、二 | **人可读镜像**——实际执行靠 `tasks.yaml` 的 `locks` |
| **§2** | 从被删的代码里摘出来的算法，归谁 | **硬规则三** | **不可替代**，没有任何机器机制管这件事 |

**§2 才是这份文件存在的真正理由。** 摘算法漏摘**不会让任何测试失败**——对应的测试跟着被删的目录一起没了，
三层约束（`CLAUDE.md` 告知 / SDD review / 验收脚本）全都挡不住。这是 `.exec/verify/common.sh`
开头写明的唯一已知盲区，`delete_plugin.sh` 的「人工必看项」也指向这里。

---

## §1 target 占用

一行一个 target。**认领与释放由 orchestrator 的 `locks` 实际执行**，本表是人可读镜像，
用于回答「现在整体在动哪些地方」以及事后追溯。

| target | 当前归属 | 分支 | 预计释放 |
|---|---|---|---|
| *（空——尚无任务开工）* | | | |

**填写约定**

- **当前归属**：任务 ID（`D-03`、`S-06`…）。只动了一部分时写 `D-04(部分)`
- **分支**：`d/<批次>-<简述>` / `s/<批次>-<简述>` / `x/<支线>`（§6.7 分支模型）
- **预计释放**：里程碑（`M2`）或 `—`（跨里程碑）
- 超过预计释放期的，**要么更新预期要么释放**——不要放着（§6.7 集成节奏）

**硬规则二**：跨 target 的动作（如 D-005 的 `git mv`：`libs/widgetutils` → `libs/global`）
必须**同时锁住两个 target**，单独一个 PR 直接进 `main`，且期间两个 target 都不能有别的线在动。

---

## §2 算法归属登记（硬规则三）

**删减线摘出来的每一份代码，在删除的同一个 PR 里登记归属。** 没登记就变孤儿——
被删掉的 target 里唯一值得留的东西，反而因为无人认领而丢失。

适用任务：**D-00**（`dockers`/`extensions` 算法挑拣）、**D-03**（摘 `libs/ui` 算法，含
`opengl/` 约 2 100 行）、**D-035**（挑 `libs/flake` 的 UI 面）。

| 摘出的文件/目录 | 原属（从哪删的） | 归入 target | 由哪批剥 | 登记 PR | 备注 |
|---|---|---|---|---|---|
| *（空——D-00 尚未开工）* | | | | | |

**填写约定**

- **摘出的文件/目录**：路径要具体到能 `test -f` 验证。整目录摘用 `dir/`，部分摘就逐个列
- **归入 target**：`kritaimage` / `kritaglobal` / 新建的 target 名
- **由哪批剥**：S 组任务 ID。`libs/ui` 摘出的算法统一归 **S-09-a**
- **登记 PR**：**必须与执行删除的是同一个 PR**——分开做就会漏
- 一个文件如果最终决定不留，**也要写一行并标 `丢弃`**，附一句理由。
  「查得到当初判过」比「查不到有没有判过」重要得多

**验收时对照**：`delete_plugin.sh` 会提示「核对候选清单是否都已落位（`notes/` 与 `OWNERSHIP.md`）」。
`notes/<target>.md` 是 agent 写的剥离笔记，本表是人确认后的结论——**两边对不上就是漏了**。

---

## 谁来写

`执行调度.md` 的三条前提之一是「agent 始终不改 `OWNERSHIP.md`、不改 `docs/`、不改验收脚本」，
但硬规则三要求登记与删除在同一个 PR 里——而删除是 agent 做的。这两条直接冲突。

**当前的处理**：

```
agent  →  status.sh note <ID> "摘出 X，建议归 Y，由 S-09-a 剥，理由 Z"
              ↓  写进 .exec/progress/<ID>.md
人     →  验收该任务时转录进本表 §2，同时确认归属判断
```

这样 agent 不碰本文件，登记也不会丢。**代价是登记与删除不在同一个 PR** ——
硬规则三的原意打了折扣，靠「验收不通过就不能置 `VERIFIED`」补上。

> **如果 D-00 实际跑起来发现转录太重**，可以改成让 agent 直接追加 §2（只追加、不改 §1），
> 或者把登记做成结构化文件由脚本汇总。**这是执行中要复盘的一个点，不是已经定死的。**
